export class OfferService {
    applyOffer(bookingId: string, discount: number) {
        console.log(`Applying discount of Rs. ${discount} to Booking Id ${bookingId}`);
        // Logic to apply discount to a booking goes here
    }
}